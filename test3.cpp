#include <iostream>
#include <map>
#include <algorithm>
#include <string.h>
#include <stdio.h>
using namespace std;
#define ll long long
const int maxn = 505;
const int maxm = 10005
map<string, i> users;

int main(int argc, char const *argv[])
{
    int n, m;
    string name;
    cin >> n >> m;
    for (int i = 0; i < n; i++) {
        cin >> name;
        users[name] = i;
    }

    for (int i = 0; i < m; i++) {
        
    }


    return 0;
}