#include <iostream>
#include <algorithm>
#include <string.h>
#include <stdio.h>
using namespace std;
#define ll long long
const int N = 105;
int n, m, s;
double dis[N], v, rate[N][N], cost[N][N];
int main(int argc, char const *argv[])
{
    while (~scanf("%d %d %d %d", &n, &m, &s, &v))
    return 0;
}